# 基金投资大师 - 联调测试准备报告

**负责人**: FE_Dev (小虾米)  
**准备日期**: 2026-03-17 20:35  
**联调阶段**: Phase 5 - API 集成  

---

## 一、前端 API 调用代码检查结果

### ✅ 已完成的 API 集成

| 接口 | 调用方式 | 状态 | 文件位置 |
|------|----------|------|----------|
| 基金信息查询 | `supabase.functions.invoke('fund-info')` | ✅ 代码就绪 | `src/pages/report/index.tsx:52-88` |
| AI 报告生成 (SSE 流式) | `fetch(.../fund-analysis)` | ✅ 代码就绪 | `src/pages/report/index.tsx:90-153` |
| Supabase 客户端配置 | `customFetch` (Taro 兼容) | ✅ 已配置 | `src/client/supabase.ts` |
| 状态管理 | Zustand `fundStore` | ✅ 已配置 | `src/store/fundStore.ts` |

### 📋 前端 API 调用细节

#### 1. 基金信息查询 (`fund-info`)
```typescript
// 调用方式
const {data, error} = await supabase.functions.invoke('fund-info', {
  body: JSON.stringify({code: input})
})

// 期望响应格式
{
  success: boolean,
  data?: {
    fund_code: string,
    fund_name: string,
    net_value: number,
    nav_date: string,
    daily_growth: number,
    daily_growth_rate: number,
    accumulated_nav: number,
    fund_type: string,
    fund_scale: number,
    fund_manager: string,
    established_date: string
  },
  error?: string,
  multiple?: boolean,
  matches?: Array<{fund_code: string, fund_name: string}>
}
```

#### 2. AI 报告生成 (`fund-analysis`)
```typescript
// 调用方式 (SSE 流式)
const response = await fetch(`${SUPABASE_URL}/functions/v1/fund-analysis`, {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    Authorization: `Bearer ${SUPABASE_ANON_KEY}`
  },
  body: JSON.stringify({fundInfo: fundData})
})

// 响应格式：text/event-stream
// data: {"choices":[{"delta":{"content":"..."}}]}
// data: [DONE]
```

---

## 二、需要后端配合的接口清单

### 🔴 阻塞接口 (P0 - 必须完成)

| # | 接口名称 | 端点 | 方法 | 优先级 | 状态 | 备注 |
|---|----------|------|------|--------|------|------|
| 1 | 基金信息查询 | `/functions/v1/fund-info` | POST | P0 | ⚠️ 待部署 | 当前为模拟数据，需对接盈米 MCP |
| 2 | AI 报告生成 | `/functions/v1/fund-analysis` | POST | P0 | ⚠️ 待部署 | 需配置阿里云百炼 API Key |

### 🟡 辅助接口 (P1 - 建议完成)

| # | 接口名称 | 端点 | 方法 | 优先级 | 状态 | 备注 |
|---|----------|------|------|--------|------|------|
| 3 | 基金搜索 | `/functions/v1/fund-search` | POST | P1 | ⚠️ 待部署 | 支持模糊搜索/拼音缩写 |
| 4 | 净值查询 | `/functions/v1/fund-nav` | POST | P1 | ⚠️ 待部署 | 历史净值数据 |

### 🟢 优化接口 (P2 - 可选)

| # | 接口名称 | 端点 | 方法 | 优先级 | 状态 | 备注 |
|---|----------|------|------|--------|------|------|
| 5 | 报告缓存查询 | `/functions/v1/fund-report-cache` | GET | P2 | - | 7 天缓存，减少 AI 调用 |
| 6 | 查询历史记录 | `/functions/v1/query-history` | GET | P2 | - | 用户查询历史 (可选) |

---

## 三、联调测试环境准备

### 3.1 环境变量配置

**前端环境变量** (`.env`):
```bash
# Supabase 配置 (待后端提供)
TARO_APP_SUPABASE_URL=https://your-project.supabase.co
TARO_APP_SUPABASE_ANON_KEY=your-anon-key

# 应用 ID
TARO_APP_APP_ID=fund-investment-master
```

**后端环境变量** (`.env.mcp`):
```bash
# 盈米 MCP 配置 (CEO 已提供)
YINGMI_MCP_ENDPOINT=https://stargate.yingmi.com/mcp/v2
YINGMI_MCP_API_KEY=FmSylSxtSrUGWWL3KWI47A

# 阿里云百炼 API (待配置)
QWEN_API_KEY=sk-xxx

# Supabase 配置
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-key
```

### 3.2 测试数据准备

#### 测试基金清单

| 基金代码 | 基金名称 | 类型 | 用途 |
|----------|----------|------|------|
| 000001 | 易方达蓝筹精选混合 | 混合型 | 核心功能测试 |
| 110022 | 华夏成长混合 | 混合型 | 核心功能测试 |
| 007119 | 天弘沪深 300ETF | 指数型 | ETF 测试 |
| 000002 | 华夏回报混合 | 混合型 | 多基金匹配测试 |
| YFD | (拼音缩写) | - | 拼音搜索测试 |

#### Mock 数据 (用于前端独立测试)

```typescript
// 基金信息 Mock
const mockFundInfo = {
  fund_code: "000001",
  fund_name: "易方达蓝筹精选混合",
  net_value: 1.2345,
  nav_date: "2026-03-17",
  daily_growth: 0.0123,
  daily_growth_rate: 1.02,
  accumulated_nav: 2.3456,
  fund_type: "混合型",
  fund_scale: 50.5,
  fund_manager: "张坤",
  established_date: "2010-01-01"
}

// AI 报告 Mock (片段)
const mockAnalysis = `## 一句话投资判断
该基金为优质蓝筹基金，长期业绩优秀，适合中长期持有。

## 基金概况与定位
- 基金类型：混合型
- 投资策略：价值投资，优选蓝筹
...`
```

---

## 四、联调测试脚本

### 4.1 后端 API 测试脚本 (curl)

```bash
#!/bin/bash
# 测试基金信息查询接口

SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_ANON_KEY="your-anon-key"

echo "=== 测试基金信息查询 ==="
curl -X POST "${SUPABASE_URL}/functions/v1/fund-info" \
  -H "Authorization: Bearer ${SUPABASE_ANON_KEY}" \
  -H "Content-Type: application/json" \
  -d '{"code": "000001"}'

echo -e "\n\n=== 测试基金搜索 (模糊) ==="
curl -X POST "${SUPABASE_URL}/functions/v1/fund-info" \
  -H "Authorization: Bearer ${SUPABASE_ANON_KEY}" \
  -H "Content-Type: application/json" \
  -d '{"code": "易方达"}'

echo -e "\n\n=== 测试 AI 报告生成 (SSE) ==="
curl -X POST "${SUPABASE_URL}/functions/v1/fund-analysis" \
  -H "Authorization: Bearer ${SUPABASE_ANON_KEY}" \
  -H "Content-Type: application/json" \
  -d '{
    "fundInfo": {
      "fund_code": "000001",
      "fund_name": "易方达蓝筹精选混合",
      "net_value": 1.2345,
      "daily_growth_rate": 1.02,
      "fund_type": "混合型",
      "fund_scale": 50.5,
      "fund_manager": "张坤"
    }
  }'
```

### 4.2 前端联调测试步骤

```bash
# 1. 安装依赖
cd /Users/jun15prolan/.openclaw/workspace/projects/基金投资大师
pnpm install

# 2. 配置环境变量
cp .env.example .env
# 编辑 .env 填入 Supabase 配置

# 3. 启动开发服务器
pnpm run dev:h5

# 4. 访问 H5 页面
# 浏览器打开：http://localhost:10086

# 5. 测试流程
# a. 输入基金代码 "000001" → 查询
# b. 检查基金信息展示 (10 个字段)
# c. 等待 AI 报告生成 (流式输出)
# d. 检查报告内容完整性
# e. 测试分享功能
# f. 测试错误处理 (输入无效代码)
```

### 4.3 联调检查清单

- [ ] **后端 API 部署**
  - [ ] Supabase Edge Functions 已部署
  - [ ] `fund-info` 接口可访问
  - [ ] `fund-analysis` 接口可访问
  - [ ] CORS 配置正确
  - [ ] 环境变量已配置

- [ ] **前端配置**
  - [ ] `.env` 文件已配置
  - [ ] Supabase 客户端初始化成功
  - [ ] 路由跳转正常
  - [ ] 状态管理正常

- [ ] **功能测试**
  - [ ] 基金代码查询成功
  - [ ] 基金名称搜索成功
  - [ ] 多基金匹配选择器正常
  - [ ] AI 报告流式输出正常
  - [ ] 报告内容完整 (6 大维度)
  - [ ] 分享功能正常
  - [ ] 错误提示友好

- [ ] **性能测试**
  - [ ] 基金查询响应 < 3 秒
  - [ ] AI 报告生成 < 30 秒
  - [ ] 首页加载 < 2 秒
  - [ ] 无内存泄漏

---

## 五、TODO.md 更新

### 当前联调准备状态

| 任务 | 状态 | 负责人 | 预计完成 |
|------|------|--------|----------|
| 前端 API 调用代码检查 | ✅ 完成 | FE_Dev | 2026-03-17 20:35 |
| 联调测试环境准备 | ✅ 完成 | FE_Dev | 2026-03-17 20:35 |
| 联调测试脚本编写 | ✅ 完成 | FE_Dev | 2026-03-17 20:35 |
| 后端 API 部署 | ⏳ 等待中 | BE_Dev | - |
| 联调测试执行 | ⏳ 待开始 | FE_Dev + BE_Dev | - |

### 阻塞问题

| # | 问题 | 影响 | 负责人 | 解决状态 |
|---|------|------|--------|----------|
| 1 | 盈米 MCP 接口对接 | `fund-info` 返回模拟数据 | BE_Dev | 🔄 进行中 |
| 2 | 阿里云百炼 API Key 配置 | `fund-analysis` 无法调用 | BE_Dev | ⏳ 待配置 |
| 3 | Supabase Edge Functions 部署 | 接口无法访问 | BE_Dev | ⏳ 待部署 |

---

## 六、下一步行动

### FE_Dev (前端)
1. ✅ 完成前端 API 调用代码检查
2. ✅ 准备联调测试脚本
3. ⏳ 等待后端 API 部署完成后执行联调
4. ⏳ 执行功能测试用例 (测试用例.md)
5. ⏳ 执行兼容性测试 (iOS/Android/微信)

### BE_Dev (后端)
1. 🔄 完成盈米 MCP 接口对接 (`fund-detail`, `fund-holding`)
2. ⏳ 配置阿里云百炼 API Key
3. ⏳ 部署 Supabase Edge Functions
4. ⏳ 提供 Supabase 项目配置 (URL + Anon Key)
5. ⏳ 配合前端进行联调测试

### PM (项目经理)
1. ✅ 提供盈米 MCP 凭证
2. ⏳ 确认阿里云百炼 API Key
3. ⏳ 协调联调测试时间
4. ⏳ 验收测试报告

---

## 七、联系方式

- **FE_Dev**: 小虾米 (前端开发)
- **BE_Dev**: 小虾米 (后端开发)
- **PM**: Blues
- **CEO**: Blues

**沟通渠道**: OpenClaw Workspace

---

*最后更新*: 2026-03-17 20:35  
*下次更新*: 后端 API 部署完成后
