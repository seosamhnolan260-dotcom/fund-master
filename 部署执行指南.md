# 🚀 基金投资大师 - 部署执行指南

**创建时间**: 2026-03-17 22:10  
**状态**: 准备就绪

---

## ✅ 已完成准备

| 项目 | 状态 |
|------|------|
| Supabase CLI 安装 | ✅ v2.75.0 |
| 项目配置 | ✅ config.toml |
| 盈米 MCP 配置 | ✅ 已确认 |
| 阿里云百炼配置 | ✅ 已确认 |
| 部署脚本 | ✅ deploy.sh |
| 前端构建 | ✅ dist/ 目录就绪 |

---

## 📋 部署步骤

### 步骤 1️⃣：登录 Supabase

```bash
cd /Users/jun15prolan/.openclaw/workspace/projects/基金投资大师
supabase login
```

这会打开浏览器让你登录 Supabase 账号。

### 步骤 2️⃣：执行部署脚本

```bash
./deploy.sh
```

脚本会自动执行：
1. ✓ 检查登录状态
2. ✓ 链接项目 (epuswribzgubnjigpcui)
3. ✓ 配置环境变量（盈米 MCP + 阿里云百炼）
4. ✓ 数据库迁移
5. ✓ 部署 7 个 Edge Functions
6. ✓ 验证部署

---

## 🔧 手动部署（备选方案）

如果脚本执行失败，可以手动执行：

### 1. 登录
```bash
supabase login
```

### 2. 链接项目
```bash
cd supabase
supabase link --project-ref epuswribzgubnjigpcui
```

### 3. 设置环境变量
```bash
# 盈米 MCP
supabase secrets set YINGMI_MCP_ENDPOINT=https://stargate.yingmi.com/mcp/v2
supabase secrets set YINGMI_MCP_API_KEY=FmSylSxtSrUGWWL3KWI47A

# 阿里云百炼
supabase secrets set QWEN_API_KEY=sk-sp-0cb0574acc0a4e47b8f184d1299dad77
supabase secrets set QWEN_BASE_URL=https://coding.dashscope.aliyuncs.com/v1
supabase secrets set QWEN_MODEL=qwen3.5-plus

# Supabase
supabase secrets set SUPABASE_URL=https://epuswribzgubnjigpcui.supabase.co
```

### 4. 数据库迁移
```bash
supabase db push
```

### 5. 部署函数
```bash
supabase functions deploy fund-search
supabase functions deploy fund-nav
supabase functions deploy fund-detail
supabase functions deploy fund-holding
supabase functions deploy fund-performance
supabase functions deploy fund-info
supabase functions deploy fund-analysis
```

### 6. 验证
```bash
supabase functions list
```

---

## 🧪 测试部署

部署完成后，测试函数：

```bash
# 测试基金搜索
curl -X POST 'https://epuswribzgubnjigpcui.supabase.co/functions/v1/fund-search' \
  -H 'Authorization: Bearer sb_publishable_RDhIc0k6OEfx5TzBS1zoWQ_TcpHhu8i' \
  -H 'Content-Type: application/json' \
  -d '{"code": "000001"}'

# 测试基金详情
curl -X POST 'https://epuswribzgubnjigpcui.supabase.co/functions/v1/fund-detail' \
  -H 'Authorization: Bearer sb_publishable_RDhIc0k6OEfx5TzBS1zoWQ_TcpHhu8i' \
  -H 'Content-Type: application/json' \
  -d '{"code": "000001"}'
```

---

## 📁 文件位置

| 文件 | 路径 |
|------|------|
| 部署脚本 | `projects/基金投资大师/deploy.sh` |
| 环境变量 | `projects/基金投资大师/.env.production` |
| Supabase 配置 | `projects/基金投资大师/supabase/config.toml` |
| 前端产物 | `projects/基金投资大师/dist/` |

---

## ⚠️ 注意事项

1. **Service Role Key**: 部署脚本中未包含，如需设置请在 Supabase Dashboard 获取后手动添加
2. **前端部署**: Edge Functions 部署完成后，前端 dist/ 目录可部署到 Vercel/Netlify
3. **测试验证**: 部署后请执行测试用例验证功能正常

---

**准备就绪，请执行 `supabase login` 后运行 `./deploy.sh`** 🚀
